import React from 'react';
import styled from '@emotion/styled';

export const ActionScreenLayout = styled('div')({
  flex: 1,
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'stretch',
  width: '100%',
  minHeight: '100vh'
});
