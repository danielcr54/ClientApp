import React from 'react';
import styled from '@emotion/styled';

export const FooterContent = styled('div')({
  width: '100%',
  display: 'flex',
  fontSize: 12,
  color: '#666'
});

export default FooterContent;

export const FooterContentStart = styled('div')({
  flex: 1,
  padding: '10px 16px'
});

export const FooterContentEnd = styled('div')({
  padding: '12px 16px'
});
