import React from 'react';
import styled from '@emotion/styled';

export const ScreenSidebar = styled('div')({
  width: 250,
  display: 'flex',
  flexDirection: 'column',
  backgroundColor: 'rgba(17, 16, 23, 0.78)'
});

export default ScreenSidebar;
